package labs_examples.objects_classes_methods.labs.oop.C_blackjack.code;

import java.util.ArrayList;

public class Deck {
    Card[] cards;
    ArrayList<Integer> usedCards;

    static void makeDeck() {
        //cycle through the suite after the full amount of cards are gone through
        for (int i = 0; i < 4; i++) {
            for (int j = 1; j < 14; j++) {

            }
        }
    }
}
